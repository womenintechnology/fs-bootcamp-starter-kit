<VirtualHost *:80>
	ServerAdmin webmaster@localhost
	DocumentRoot /srv/public
	<Directory /srv/public>
        Order deny,allow
        Allow from all
		Require all granted
		AllowOverride All
    </Directory>
</VirtualHost>
